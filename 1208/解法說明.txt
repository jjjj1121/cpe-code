使用pirm's演算法完成mst

最後在使用

STL :sort 裡面的compare自定義成題目要求的輸出即可 

prim's 演算法:

選一個點當起始點，開始往下一步走，將下一步可到的儲存在一個Vector裡面

然後找最小且不會造成cycle的邊 將其存在vector ans裡面

然後check[i]=1確保此點不會被重複走過

當check 的size等於 城市的數量 也就是n時就結束演算法。