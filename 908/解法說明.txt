將輸入存在二維vector map裡面

使用vector check 檢查此點有沒有走過

使用prim 演算法完成此題

首先選擇map裡的第一個點當作起始點

開始往外擴散

看這點可以1步走到哪些點

將這些點和weight 存在 vector temp裡面

再去temp裡面找哪個weight最小

將此邊選擇 

並從temp裡移除,check[此點] =1

然後坐下一個點

直到我們的check裡面的點都走完

即可算出最小的消耗